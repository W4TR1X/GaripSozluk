@model IEnumerable<GaripSozluk.Common.ViewModels.Api.ApiResultVM>

@{
    ViewData["Title"] = "ApiResult";
}

<h1>ApiResult</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QueryString)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SearchType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.QueryString)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SearchType)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
}
    </tbody>
</table>
